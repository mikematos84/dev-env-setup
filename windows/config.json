{
  "buckets": [
    "extras"
  ],
  "dependencies": [
    {
      "name": "git",
      "description": "Git",
      "required": true
    },
    {
      "name": "sudo",
      "description": "Elevated privileges utility",
      "required": false
    }
  ],
  "devDependencies": [
    {
      "name": "nvm",
      "description": "Node Version Manager",
      "required": true,
      "postInstall": "nvm install lts; nvm use lts"
    },
    {
      "name": "mkcert",
      "description": "Local development certificates",
      "required": false
    },
    {
      "name": "yarn",
      "description": "Node.js dependency manager",
      "required": false
    },
    {
      "name": "pnpm",
      "description": "Fast, disk space efficient package manager for Node.js",
      "required": true
    },
    {
      "name": "vscode",
      "description": "Visual Studio Code editor",
      "required": false
    },
    {
      "name": "zed",
      "description": "Zed code editor",
      "required": false
    },
    {
      "name": "cursor",
      "description": "Cursor AI-powered code editor",
      "required": true
    }
  ],
  "system": {
    "sshAgent": {
      "enabled": true,
      "autoStart": true
    },
    "git": {
      "configureSSH": true,
      "description": "Configure Git with SSH integration and custom settings",
      "config": {
        "global": {
          "init": {
            "defaultBranch": "main"
          },
          "user": {
            "name": "John Doe",
            "email": "john.doe@example.com"
          },
          "core": {
            "sshCommand": "C:/Windows/System32/OpenSSH/ssh.exe"
          },
          "push": {
            "autoSetupRemote": true
          }
        }
      }
    }
  }
}

